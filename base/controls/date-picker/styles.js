import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  contentStyles: {
    maxHeight: '85%',
    minHeight: '50%',
  },
  actionsWrapper: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
});
