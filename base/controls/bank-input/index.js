import React from 'react';
import {View} from 'react-native';
import SelectList from '../select-list';
const BankInput = ({primary, secondary}) => {
  return <SelectList primary={primary} secondary={secondary} />;
};

export default BankInput;
