export default ({palette}) => ({
  emptyText: {
    color: palette.gray,
    textAlign: 'center',
  },
  emptyWrapper: {
    alignItems: 'center',
  },
  filter: {
    marginBottom: 20,
  },
  root: {
    flex: 1,
    paddingVertical: 20,
    paddingHorizontal: 20,
  },
});
